import {useSelector} from 'react-redux'
import {rootState} from '../store/store'

// export const useTypedSelector = useSelector(rootState)
// export const useTypedSelector = useSelector(rootState.filterGroups)